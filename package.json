{
  "name": "gotium-multistep-component",
  "version": "0.0.1",
  "description": "Generic multistep form component",
  "main": "src/app.js",
  "dependencies": {
    "react": "^0.12.2"
  },
  "devDependencies": {
    "expect.js": "^0.3.1",
    "reactify": "latest",
    "uglifyjs": "latest"
  },
  "scripts": {
    "watch": "watchify -v ./src/js/app.js -o ./build/js/bundle.js",
    "build": "browserify ./src/js/app.js -o ./src/js/bundle.js",
    "publish": "browserify ./src/js/app.js | uglifyjs -cm > ./build/js/bundle.js && cp html+css",
    "test": "echo \"Todo! no test specified\" && exit 1"
  },
  "author": "@djidja8",
  "license": "MIT",
  "browserify": {
    "transform": [
      [
        "reactify",
        {
          "es6": true
        }
      ]
    ]
  }
}
