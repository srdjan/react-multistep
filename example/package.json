{
  "name": "react-multistep-example",
  "homepage": "https://srdjan.github.io/react-multistep",
  "version": "1.0.1",
  "license": "MIT",
  "scripts": {
    "debug": "esbuild app.js --bundle --format=cjs --outfile=bundle.js --loader:.js=jsx --sourcemap --preserve-symlinks",
    "build": "esbuild app.js --bundle --format=cjs --outfile=bundle.js --loader:.js=jsx",
    "start": "servor . index.html 8080 --reload --browse --static"
  },
  "alias": {
    "react": "./node_modules/react"
  },
  "dependencies": {
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "esbuild": "0.12.22"
  }
}
