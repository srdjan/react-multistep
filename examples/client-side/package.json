{
  "name": "react-multistep-example",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "esbuild app.tsx --bundle --format=esm --outdir=./dist --alias:react-multistep=../../src/index.ts --jsx=automatic --loader:.css=css --minify --define:process.env.NODE_ENV='\"production\"'",
    "dev": "esbuild app.tsx --bundle --format=esm --outdir=./dist --alias:react-multistep=../../src/index.ts --jsx=automatic --loader:.css=css --sourcemap --watch --servedir=.",
    "start": "npm run dev",
    "clean": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true });\""
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.10"
  }
}
